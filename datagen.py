def generate_samples(rho, N, how='complete'):
    """ Generate samples from the state rho.
        rho: a qutip state.
        N: how many measurements to take 
        how: a measurement strategy. Choices:
            'complete': all 3^L pauli bases are used
            'random': use random local rotations
        """
    pass